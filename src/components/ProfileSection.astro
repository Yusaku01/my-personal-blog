---
interface Props {
  title: string;
  className?: string;
}

const { title, className = "mb-8" } = Astro.props;
---

<div class={className}>
  <h2 class="text-xl font-semibold mb-4">{title}</h2>
  <slot />
</div>
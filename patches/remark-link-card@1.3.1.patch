diff --git a/index.js b/index.js
index 5de58d63759383c8644e1a982b63eb85f838a19b..ef0134ea7421bba27d108b93b0bc6f74a06ee182 100644
--- a/index.js
+++ b/index.js
@@ -12,7 +12,7 @@ const defaultOutputDirectory = '/remark-link-card/';
 const rlc = (options) => {
   return async (tree) => {
     transformers = [];
-    visit(tree, 'paragraph', (paragraphNode, index) => {
+    visit(tree, 'paragraph', (paragraphNode, index, parent) => {
       if (paragraphNode.children.length !== 1) {
         return tree;
       }
@@ -39,16 +39,19 @@ const rlc = (options) => {
             };
 
             // Replace paragraph node with linkCardNode
-            tree.children.splice(index, 1, linkCardNode);
+            const targetChildren =
+              parent && Array.isArray(parent.children)
+                ? parent.children
+                : tree.children;
+            targetChildren.splice(index, 1, linkCardNode);
           });
         }
       });
     });
 
     try {
       await Promise.all(transformers.map((t) => t()));
-    } catch (error) {
-      console.error(`[remark-link-card] Error: ${error}`);
+    } catch {
+      // Keep markdown rendering resilient when metadata fetches fail.
     }
 
     return tree;
@@ -57,10 +60,7 @@ const rlc = (options) => {
 const getOpenGraph = async (targetUrl) => {
   try {
     const { result } = await ogs({ url: targetUrl, timeout: 10000 });
     return result;
-  } catch (error) {
-    console.error(
-      `[remark-link-card] Error: Failed to get the Open Graph data of ${error.result.requestUrl} due to ${error.result.error}.`
-    );
+  } catch {
     return undefined;
   }
 };
